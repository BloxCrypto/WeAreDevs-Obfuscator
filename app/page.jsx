export default function Page() {
  return (
    <main style={{fontFamily: 'system-ui, -apple-system, Segoe UI, Roboto, sans-serif', padding: 24}}>
      <h1>WeAreDevs Obfuscator</h1>
      <p>Minimal Next.js app page — your static web UI remains in the <code>/web</code> folder.</p>
      <p>If you want the original static UI served by Next, move `/web` into `/public`.</p>
    </main>
  )
}
